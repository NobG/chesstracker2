# Legacy Java service example only.
# The recommended production setup uses the root .env.example with Docker Compose.
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/chesstracker
SPRING_DATASOURCE_USERNAME=chesstracker
SPRING_DATASOURCE_PASSWORD=change-me
